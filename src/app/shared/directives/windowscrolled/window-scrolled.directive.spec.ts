import { WindowScrolledDirective } from './window-scrolled.directive';

describe('WindowScrolledDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowScrolledDirective();
    expect(directive).toBeTruthy();
  });
});
